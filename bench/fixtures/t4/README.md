# Board fixture

This task has no filesystem seed. The benchmark runner creates the three `miniTickets` from
`bench/tasks/t4-board-drain.json` on an isolated project in the local ticket-board service.
